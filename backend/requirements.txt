# Dependency specification for Social Network backend
#
# Stack:
# - Python 3.12
# - Django backend-first architecture
# - Django REST Framework
# - JWT authentication
# - PostgreSQL (production) / SQLite (development)
# - Explicit domain modeling
#
# Rules:
# - Only production-relevant dependencies
# - No dev tools unless strictly necessary
# - No duplicate or overlapping libraries
#
# Required capabilities:
# - REST API
# - JWT auth
# - CORS support
# - Image/media handling
# - Environment variable management

# Core framework
Django==4.2.8

# REST API
djangorestframework==3.14.0

# JWT Authentication
# 5.3.0 importava `pkg_resources` no __init__. O setuptools removeu esse
# módulo, então instalar as versões pinadas hoje quebra no import — antes de
# qualquer teste rodar. 5.5.1 não depende mais dele.
djangorestframework-simplejwt==5.5.1

# Database
psycopg2-binary==2.9.9

# CORS
django-cors-headers==4.3.1

# Media handling
Pillow==10.1.0

# Environment variables
python-dotenv==1.0.0
